class main() {
    # A var declared without a value must be assigned before it's used.
    var bob: str;
    # input() prints the prompt (no trailing newline) and reads a line.
    bob = input("What is your name? ");
    print(bob);
}
