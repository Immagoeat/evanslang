class main() {
    # str and int are two of the four supported types (float and bool
    # are the other two - see floats_and_bools.el).
    var EXAMPLE: str = "Hello";
    var COUNT: int = 9;
    print(EXAMPLE);
    print(COUNT);
}
